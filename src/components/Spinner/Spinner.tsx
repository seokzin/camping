import { Layout, Content, SpinRing } from './Spinner.styled';

const Spinner = () => {
  return (
    <Layout>
      <Content>
        <SpinRing />
      </Content>
    </Layout>
  );
};

export default Spinner;
